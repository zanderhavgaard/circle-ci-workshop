jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run: 
          name: Run and build.
          command: gradle test
          command: gradle jar
      - store_test_results:
          path: build/test-results
